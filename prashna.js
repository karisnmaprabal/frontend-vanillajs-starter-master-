
/*console.log("     ")
for (let i = 0; i < myArray.length; i++) {

    console.log(`Index: ${i}, Value in UPPERCASE/CAPITAL: ${myArray[i].toUpperCase()}`);
    // console.log('New Index:' + i, 'New Value' + myArray[i]);
}

console.log("     ")

// I am done with my work, now time for callback 
callback;


// this logs out the array items starting from the last index
const callFromLast = (myArray) => {

    console.log("     ");
    for (let i = myArray.length - 1; i >= 0; i--) {

        console.log(`Index Last: ${i}, Value in lowercase/small: ${myArray[i].toLowerCase()}`);
        // console.log('New Index:' + i, 'New Value ' + myArray[i]);

    }
}

callFromFirst(myArray, callFromLast(myArray));

/* TODO : how to use nodemon to see the changes as you save in IDE 
install nodemon globally    =>   npm i -g nodemon
instead of doing node prashna, we can just do nodemon prashna 
this way we do not have to restart the server again on changes!

......................................................................

how to be productive by  downloading the existing github projects ( like starter/boilerplates)
=> " We do not want to reinvent the wheel "  => we need to rese the existing stuffs avaiable online 
...

......................................................................

call back function => purpose => how to use 
for further reference check it online ! 

-----------------------------------------------------------------------
 everything has to be properly commented 





const myArray = ['ram', 'shyam', 'hari', "Krishna"];

// this logs out the array items starting from the first index
const callFromFirst = (myArray, callback) => {*/








console.log("This Is My First website")

const name = ["sita", "rita", "meena", "preeta", "meeta"]
const marks = [1, 2, 3, 4, 5, 6, 7, 8, 9]
const mixed = ["string", 1, 2, 3, [1, 2, 3]];
const arr = new Array(1, 2, 3, 4, 5, 'preeta');
console.log("arr");
console.log(name)

